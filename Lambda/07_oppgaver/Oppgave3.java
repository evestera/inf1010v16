import java.util.*;

class Oppgave3 {
    public static void main(String[] args) {
        List<Integer> numbers = new LinkedList<>();
        for (int i = 0; i < 20; i++) {
            numbers.add(i);
        }

        // Ta vekk alle partall
        // hint: List har en metode removeIf

        // Bytt ut tallene med kvadratet (n * n) av dem
        // hint: List har en metode replaceAll

        List<String> numberStrings = new LinkedList<>();
        // Konvertere tallene til tekst og legg dem til med
        // numbers.forEach(...);

        // Sorter tallene reversert

        // Skriv ut tallene med
        // numberStrings.forEach(...);
    }
}
